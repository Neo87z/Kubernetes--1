apiVersion: apps/v1
kind: Deployment
metadata:
  generation: 1
  labels:
    app: last-resort
  name: repo1done
  namespace: default
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: last-resort
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: last-resort
    spec:
      containers:
        - image: neo87z/repo1:1e05bebb1c48c71eb4a6ddcdb8ea615abebf3ba7
          imagePullPolicy: Always
          name: repo1
          resources: {}
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          ports:
            - containerPort: 3000
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30